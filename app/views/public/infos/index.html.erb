<!--投稿の一覧ページ-->
<div class='container px-5 px-sm-0' >
  <div class='row'>
    <!--サイドバー-->
    <div class='side-bar col-md-3 mt-5'>
      <%= render 'public/users/sidebar', info: @info, user: current_user, genres: @genres, bookmarks: @bookmarks %>
    </div>

    <!--投稿の一覧-->
    <div class='col-md-8 offset-md-1 mt-4'>

    <%= link_to 'タグ一覧', info_tags_path(tag) %>

      <h3 class="mb-3">情報一覧</h3>
      <%= render 'index', infos: @infos %>
    </div>
  </div>
</div>

