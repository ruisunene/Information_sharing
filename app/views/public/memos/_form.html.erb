<%= form_with(model:[info, memo], local: false) do |f| %>
  <%= f.text_area :memo, rows:'5',placeholder: "メモ・やるべきことを記入", class: "w-100" %>
  <%= f.submit "送信する", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>